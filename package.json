{
  "name": "postmessagepoc",
  "version": "1.0.0",
  "description": "Post Message API POC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npm-run-all --parallel serve:*",
    "serve:a": "serve --symlinks src/example-a -p 8001",
    "serve:b": "serve -s src/example-b -p 8002",
    "serve:u": "serve -s src/example-u -p 8003"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "serve": "^14.0.1"
  }
}
